#include<stdio.h>
#include<string.h>
 int ar[100],pr[100],mx;
 void bin(int n)
 {
     int m,h,l;
     if(t)
 }
int main()
{
    int n,i,m,h,l=1,j,k,t;

    memset(ar,0,sizeof(ar));
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&t);
       bin(t,0,mx);
    }

}
