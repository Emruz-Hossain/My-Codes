#include<stdio.h>
#include<string.h>
#include<algorithm>
using namespace std;
int main()
{
    char a[100];
    int l;
    scanf("%s",&a);
    l=strlen(a);
    sort(a,a);
    sor
    puts(a);
    return 0;
}
